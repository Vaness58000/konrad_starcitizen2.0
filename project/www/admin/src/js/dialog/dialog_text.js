"use strict";
/*let elementDialogMsg = document.querySelector("#dialog-msg");
let reponseDialogMsg = false;

function showDialogText(message) {
  reponseDialogMsg = false;
  elementDialogMsg.querySelectorAll("#dialog-text").innerHTML = message;
}

function validationFormDialogMsg(e) {
  e.preventDefault();
  reponseDialogMsg = true;
  elementDialogMsg.close();
}

function closeDialogMsg(e) {
  e.preventDefault();
  elementDialogMsg.close();
}

if (elementDialogMsg != undefined) {
  elementDialogMsg.querySelectorAll(".close-dialog").forEach((element) => {
    element.addEventListener("click", closeDialogMsg);
  });
  elementDialogMsg.querySelectorAll(".validation-dialog").forEach((element) => {
    element.addEventListener("click", validationFormDialogMsg);
  });
}*/
